import { AnotheroneMiddleware } from './anotherone.middleware';

describe('AnotheroneMiddleware', () => {
  it('should be defined', () => {
    expect(new AnotheroneMiddleware()).toBeDefined();
  });
});
